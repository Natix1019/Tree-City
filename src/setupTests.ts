import '@testing-library/jest-dom'; // Import for extended DOM assertions

// You can add global mocks or configurations here
globalThis.someGlobalFunction = () => {
    // Mock implementation
};

// Example of setting up global variables
globalThis.myGlobalVariable = 'value';

